# Leave empty in local development to use the Vite /api proxy.
# For a deployed frontend, set this to the public backend URL.
VITE_API_URL=

# Local Vite proxy target used during development.
VITE_API_TARGET=http://localhost:5000
